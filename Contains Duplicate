class Solution {
    public boolean containsDuplicate(int[] nums) {
        boolean x;
        Set<Integer> set=new HashSet<>();
        for(int i=0;i<nums.length;i++)
        {
            x=set.add(nums[i]);
            if(x==false)
            {
                return true;
            }
        }
        return false;
    }
}
